# Never commit any private key material.
*.key
*.SECRET
*.pem
registry-signing-key*
*author*.key

# Build output (regenerated by publish.yml; never tracked).
/catalog.json
catalog.json

# Node / OS noise.
node_modules/
.DS_Store
